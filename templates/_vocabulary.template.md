# Vocabulary — closed tag list

The closed set of capability tags for {{REPO_NAME}}. Cards may only use tags
from this list (`kb-validate` enforces it). New tags enter **by PR only** —
never invented ad hoc by a human or an AI mid-task.

Format rule (parsed by `scripts/kb/`): every tag is a bullet starting with
`` - `tag` `` — lowercase, digits and hyphens only.

<!-- TEMPLATE NOTES (delete when filled):
     20–50 tags, written BY A HUMAN in the team's real language — an LLM will
     invent plausible tags that subtly mismatch how you think, and every
     downstream classification inherits the error. The bootstrap prompt may
     DRAFT this list from the repo inventory, but a human approves every tag
     before the first card is written. One card will own each tag. -->

## Tags

- `{{TODO: tag}}` — {{TODO: one-line meaning}}

## Synonyms / glossary

- {{TODO: "checkout = purchase-flow" style equivalences the team actually uses}}
